import React from 'react';
type AvatarProps = {
    avatarId: string;
    size?: number;
    colourHex?: string;
    showRing?: boolean;
};
export default function Avatar({ avatarId, size, colourHex, showRing, }: AvatarProps): React.JSX.Element;
export {};
//# sourceMappingURL=Avatar.d.ts.map