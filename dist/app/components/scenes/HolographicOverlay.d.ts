import React from 'react';
type HolographicOverlayProps = {
    children: React.ReactNode;
    width: number;
    height: number;
    timerPercent: number;
    isCurrentTurn: boolean;
};
export declare function HolographicOverlay({ children, width, height, timerPercent, isCurrentTurn, }: HolographicOverlayProps): React.JSX.Element;
export {};
//# sourceMappingURL=HolographicOverlay.d.ts.map